package week1
/*
n! for a number is a recursive problem

factorial(x) is if x = 0 then 1 else factorial (x-1)

there is a difference between GCD and factorial because in factorial the result of the last executed fun
*/
object Factorial {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(281); 
  println("Welcome to the Scala worksheet")}
}
